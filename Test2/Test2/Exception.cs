using System;

class Exception
{
    /*
    public static void Main(string[] args)
    {
        string[] arr = { "aaaa", "bbbb", null, "dddd" };

        Console.WriteLine("Enter an index to retrieve a name:");
        int index = Convert.ToInt32(Console.ReadLine());

        
        foreach (string name in arr)
        {
            if (name != null && name.Length > 0)
            {
                Console.WriteLine("Name: " + name);
            }
            else
            {
                Console.WriteLine("Name is empty.");
            }
        }

     
        try
        {
            Console.WriteLine("Value at index " + index + ": " + arr[index]);
        }
        catch (IndexOutOfRangeException ex)
        {
            Console.WriteLine("IndexOutOfRangeException: please enter  index between 0 to 3" );
        }
        finally
        {
            Console.WriteLine("Hope this is up to the mark ");
        }

    }*/
}

